import {FETCH_SURVEYS} from '../actions/types'

export default function(state = [], action) {
    switch (action.type) {
        case value:
            return action.payload
        default:
            return state
    }
}
